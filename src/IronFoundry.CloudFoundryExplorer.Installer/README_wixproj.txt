Note:

The wixproj file has been edited manually in ways that are not supported by the visual designer.

<OutputName /> is Platform-dependent.

<OutputPath /> and <IntermediateOutputPath /> are moved to the top-level <PropertyGroup /> since they should have the same values for all solution configurations.
